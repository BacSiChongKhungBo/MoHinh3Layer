namespace DAL
{
    //PROJECT DAL:
    // tầng 1 trong mô hình 3 lớp
    // Các tầng còn lại gồm có BUS(class libary)
    // và PRL (winform app)
    // TÁC DỤNG: THực tế, DB có thể sẽ update, sửa liên tục
    // => phải để DBCOntext + domainclass ra 1 project riêng
    // giúp: dễ cô lập để xử lý mà ko ảnh hưởng code ở tầng khác

    public class Class1
    {

    }
}
